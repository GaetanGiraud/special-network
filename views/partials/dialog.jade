div(class="modal-header"
	h1 {{ title }}

div(class="modal-body")
	p
    |{{ message }}

div(class="modal-footer")
	button(ng-repeat="btn in buttons", ng-click="close(btn.result)", class="btn", ng-class="btn.cssClass") {{ btn.label }}
